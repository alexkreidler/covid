<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta http-equiv="X-UA-Compatible" content="ie=edge" />
    <meta name="description" content="COVID-19 Testing provides an in-depth explanation of different COVID-19 testing methods, including diagnostic tests that are likely more effective than the existing widely used PCR tests. It also describes ways to scale up testing in developed and deloping countries." />
    <title>{{ title }}</title>
    <link href="https://fonts.googleapis.com/css?family=EB+Garamond&display=swap" rel="stylesheet"> 
    <link rel="stylesheet" href="./styles/common.css" />
    <link rel="stylesheet" href="./styles/homepage.css" />
    <!-- Global site tag (gtag.js) - Google Analytics -->
    <script async src="https://www.googletagmanager.com/gtag/js?id=UA-161730399-2"></script>
    <script>
      window.dataLayer = window.dataLayer || [];
      function gtag(){dataLayer.push(arguments);}
      gtag('js', new Date());

      gtag('config', 'UA-161730399-2');
    </script>
  </head>
  <body>
    <div class="container">
      <div class="main">
        {{ content }}
      </div>
      <h3 class="category">Posts</h3>
      <ul class="posts">
      {% for post in collections.posts reversed %}
        <li class="post">
          <span class="post-title">
            {% if post.data.externalLink %}
              <a href="{{ post.data.externalLink }}" target="__blank">{{ post.data.title }}</a>
            {% else %}
              <a href="{{ post.url }}">{{ post.data.title }}</a>
            {% endif %}
          </span>
          <span class="post-date">
            {{ post.date | date: "%b %Y" | downcase }}
          </span>
        </li>
      {% endfor %}
    </ul>
    </div>
  </body>
</html>
